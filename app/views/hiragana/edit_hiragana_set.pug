extends ../layout

block content
  h1 Edit Set
  | &nbsp;
  if set.length === 0
    p No flashcards available for this set.
  else
    table
      thead
        tr
          th Romaji
          th Hiragana
          th Actions
      tbody
        each flashcard in set
          tr
            td= flashcard.romaji
            td= flashcard.hiragana
            td
              button(onclick=`deleteFlashcard('${flashcard.id}')`) Delete
              | &nbsp;
              button(onclick=`editFlashcard('${flashcard.id}')`) Edit

    script(src="../js/hiragana_converter.js")